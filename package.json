{
  "name": "sane-fetch",
  "description": "A sane fetch wrapper.",
  "repository": "git://github.com/joshrtay/sane-fetch.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "make build",
    "postpublish": "make clean",
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "isomorphic-fetch": "^2.2.1"
  },
  "devDependencies": {
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-register": "^6.3.13",
    "babel-polyfill": "^6.3.13",
    "tape": "^4.2.2"
  }
}
